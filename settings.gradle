plugins {
    // Allows Gradle to automatically download a matching JDK toolchain
    // if the build machine does not already have JDK 21 installed.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'narrative-rpg'
